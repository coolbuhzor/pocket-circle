# Server-only — never prefix with NEXT_PUBLIC_
# Browser never sees this; Route Handlers proxy to the NestJS API.
# Origin only (no /api/v1) — the BFF appends the versioned prefix.
BACKEND_URL=http://localhost:4001
